# Calendars — Frontend

## JS

- `resources/js/tenant/pages/appointments.js`
  - Arquivo existente (não inspecionado) usado pelo calendário via `AppointmentController::events`.
  - Responsável pelo comportamento da agenda em torno do componente de calendário.

- Arquivo dedicado `calendars.js`:
  - (não identificado no código).

## CSS

- `resources/css/tenant/pages/appointments.css`
  - Arquivo existente (não inspecionado) com estilos para a página de agendamentos/calendário.

- Arquivo dedicado `calendars.css`:
  - (não identificado no código).

## Observações

- Padrões globais de UI (Grid.js, dark mode, TailAdmin) estão em `docs/00-global/03-padroes-frontend.md`.
