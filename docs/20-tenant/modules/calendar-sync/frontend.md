# Calendar Sync — Frontend

## JS

- Arquivo JS dedicado em `resources/js/tenant/pages/*` para `calendar-sync`:
  - (não identificado no código).

## CSS

- Arquivo CSS dedicado em `resources/css/tenant/pages/*` para `calendar-sync`:
  - (não identificado no código).

## Observações

- As views de `calendar-sync` seguem o padrão de telas administrativas do Tenant, mas não há JS/CSS específicos identificados.
- Padrões globais de UI (Grid.js, dark mode, TailAdmin) estão documentados em `docs/00-global/03-padroes-frontend.md`.
