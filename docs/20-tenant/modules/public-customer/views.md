# Public Customer — Views

Fontes: controllers públicos do Tenant.

## Views principais (agendamento público)

- Caminhos exatos de views públicas não foram inspecionados em detalhes.
- (não identificado no código — provável em `resources/views/tenant/public/*` ou subpastas específicas de agendamento público).

## Views de formulários públicos

- As views de formulários públicos são referenciadas por `PublicFormController`, mas os caminhos exatos não foram inspecionados.
- (não identificado no código — provável em `resources/views/tenant/public/forms/*` ou similar).

## Observações

- Este módulo agrupa apenas rotas e controllers públicos; a estrutura de views segue convenções da área pública do Tenant.
- Padrões globais de UI continuam válidos (ver `docs/00-global/03-padroes-frontend.md`).
