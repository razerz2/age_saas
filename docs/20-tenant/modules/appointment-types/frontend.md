# Appointment Types — Frontend

## JS

- Arquivo JS dedicado em `resources/js/tenant/pages/*` para `appointment-types`:
  - (não identificado no código).

## CSS

- Arquivo CSS dedicado em `resources/css/tenant/pages/*` para `appointment-types`:
  - (não identificado no código).

## Observações

- O módulo expõe um endpoint `grid-data` consumido por Grid.js, mas não foi identificado um arquivo JS de página específico para `appointment-types`.
- Padrões globais de UI (Grid.js, dark mode, TailAdmin) estão documentados em `docs/00-global/03-padroes-frontend.md`.
