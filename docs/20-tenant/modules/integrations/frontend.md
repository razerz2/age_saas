# Integrations — Frontend

## JS

- Arquivo JS dedicado em `resources/js/tenant/pages/*` para `integrations`:
  - (não identificado no código).

## CSS

- Arquivo CSS dedicado em `resources/css/tenant/pages/*` para `integrations`:
  - (não identificado no código).

## Observações

- O módulo segue o padrão de telas CRUD do Tenant, sem JS/CSS de página específicos identificados.
- Padrões globais de UI (Grid.js, dark mode, TailAdmin) estão documentados em `docs/00-global/03-padroes-frontend.md`.
