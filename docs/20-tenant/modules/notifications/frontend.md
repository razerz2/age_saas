# Notifications — Frontend

## JS

- Arquivo JS dedicado em `resources/js/tenant/pages/*` para `notifications`:
  - (não identificado no código).

## CSS

- Arquivo CSS dedicado em `resources/css/tenant/pages/*` para `notifications`:
  - (não identificado no código).

## Observações

- O módulo expõe um endpoint JSON (`notifications/json`) para consumo em frontends (ex.: badge de notificações), mas não há um arquivo JS de página específico identificado neste repositório.
- Padrões globais de UI (Grid.js, dark mode, TailAdmin) estão documentados em `docs/00-global/03-padroes-frontend.md`.
