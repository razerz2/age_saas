# Business Hours — Frontend

## JS

- Arquivo JS dedicado em `resources/js/tenant/pages/*` para `business-hours`:
  - (não identificado no código).

## CSS

- Arquivo CSS dedicado em `resources/css/tenant/pages/*` para `business-hours`:
  - (não identificado no código).

## Observações

- O módulo utiliza Grid.js via resposta JSON de `gridData`, mas não foi identificado um JS de página específico para `business-hours`.
- Padrões globais de UI (Grid.js, dark mode, TailAdmin) estão documentados em `docs/00-global/03-padroes-frontend.md`.
